# give all pip what you wnat to install
# enter cmd in cmd promot
# without giving version also it works

# pip install -r requirement.txt

# Core testing libraries
pytest==9.0.2
pytest-order==1.3.0
pytest-html==3.2.0
pytest-playwright==0.7.2
pytest-xdist==3.8.0
pytest-repeat==0.9.4
pytest-asyncio==0.21.0






# Browser automation
playwright==1.58.0

# Environment variable loader (for LT_USERNAME / LT_ACCESS_KEY)
python-dotenv==1.1.0

# HTTP requests (if your tests use APIs)
requests==2.32.3

# Optional extras for logging / reporting
python-json-logger==3.2.1
allure-pytest==2.15.3

